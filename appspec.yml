version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/
hooks:
  AfterInstall:
    - location: install  
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start_apache2.sh 
      timesout: 300
      runas: root     
